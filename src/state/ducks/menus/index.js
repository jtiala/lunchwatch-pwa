import reducer from './reducers';

import * as menusSelectors from './selectors';
import * as menusOperations from './operations';
import * as menusTypes from './types';

export {
  menusSelectors,
  menusOperations,
  menusTypes,
};

export default reducer;
