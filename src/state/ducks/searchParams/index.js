import reducer from './reducers';

import * as searchParamsSelectors from './selectors';
import * as searchParamsOperations from './operations';
import * as searchParamsTypes from './types';

export {
  searchParamsSelectors,
  searchParamsOperations,
  searchParamsTypes,
};

export default reducer;
